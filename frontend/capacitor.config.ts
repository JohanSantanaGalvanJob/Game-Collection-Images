import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend-ionic-game-collection',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
